# CASE latex.text_macros
# Fictional regression input for tests/COVERAGE.md: valid, and kept in the
# invalid corpus so the valid corpus output is unchanged.
bib_dir: "."
bib_files:
  - name: "macros.bib"
    category: "Papers"
